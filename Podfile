# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'vapeon' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for vapeon

  pod 'Firebase/Core'
  pod 'Firebase/Auth'

  target 'vapeonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'vapeonUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
